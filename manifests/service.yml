apiVersion: v1
kind: Service
metadata:
    name: alishakolisecureinlinescanexamples
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: alishakolisecureinlinescanexamples